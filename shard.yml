name: secrets
version: 0.1.0

authors:
  - Chris Larsen <clarsendev@protonmail.com>

description: Encrypted credentials management system, largely based on Rails/secrets

development_dependencies:
  minitest:
    github: ysbaddaden/minitest.cr
    version: "0.5.1"

targets:
  secrets:
    main: src/secrets.cr

crystal: 0.35.1

license: MIT
